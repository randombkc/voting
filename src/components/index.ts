/**
 * Shared UI components for the Perizia-Crux Voting Platform.
 *
 * Components will be implemented here as the UI is built in future steps.
 * Examples: Button, Input, Card, Badge, Modal, Navigation.
 */

export {};
